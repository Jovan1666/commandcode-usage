# 跨平台仓库：默认文本文件统一用 LF 存储。
# 否则 Windows 检出时会变 CRLF，而 shell 块（commands/*.md 里的 bash）和
# 带 shebang 的脚本对行尾敏感。
* text=auto eol=lf

# 二进制文件不要碰
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary

# Windows 批处理必须是 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 锁文件不参与 diff 统计
package-lock.json -diff linguist-generated
